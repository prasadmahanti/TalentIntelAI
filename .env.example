# Optional: paste an OpenAI key here to enable real AI suggestions.
# WARNING: Anything in VITE_* is bundled into the client and visible to users.
# Prefer the in-app "API Key" field which keeps the key in localStorage on the user's machine.
VITE_OPENAI_API_KEY=
VITE_OPENAI_MODEL=gpt-4o-mini
